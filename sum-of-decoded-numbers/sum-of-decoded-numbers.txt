long power = calculatePower(x, y, MOD);
            sum = (sum + power) % MOD;
        }
        return (int)sum;
    }
    private long calculatePower(long x, long y, long MOD) {
        long result = 1;
        while (y > 0) {
            if (y % 2 == 1) {
                result = (result * x) % MOD;
            }
            x = (x * x) % MOD;
            y = y / 2;
        }
        return result;
    }
}