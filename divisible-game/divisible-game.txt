class Solution {
    static long MOD = 1000000007;
    static int[] spf = new int[1000001];
    static {
        for (int i = 2; i <= 1000000; i++) spf[i] = i;
        for (int i = 2; i <= 1000000; i++) {
            if (spf[i] == i) {
                for (int j = 2 * i; j <= 1000000; j += i) {
                    if (spf[j] == j) spf[j] = i;
                }
            }
        }
    }

    public int divisibleGame(int[] nums) {
        long maxDiff, maxK = 2, n = nums.length;
        maxDiff = Integer.MIN_VALUE;
        TreeSet<Integer> divisors = new TreeSet<>();
        divisors.add(2);
        for (int i : nums) {
            while (i > 1) {
                divisors.add(spf[i]);
                i /= spf[i];
            }
        }

        int aliceSum = 0, bobSum = 0;
        for (Integer i : divisors) {
            aliceSum = bobSum = 0;
            for (int j = 0; j < n; j++) {
                if ((nums[j] % i) == 0) aliceSum += nums[j];
                else bobSum += nums[j];