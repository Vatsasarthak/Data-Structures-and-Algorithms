}
    }
    
    public int top() {
            min =2L * min - n;
        long n=st.peek();
        st.pop();
        if(n < min){
        if(st.isEmpty()){
            return;
        }
    }
    
    public void pop() {
            }
        }
                min = value;
                st.push(2L * value - min);
                 //yaha hum log modified value daalenge 
            else{
            }
            if(value > min){
                st.push((long)value);
        else{
        }
            min = value;
            st.push((long)value);
        if(st.isEmpty()){
    public void push(int value) {
    
    }
        
    public MinStack() {
    long min = 1000000000;